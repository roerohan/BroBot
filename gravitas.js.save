var MongoClient = require('mongodb').MongoClient;
var url = "mongodb://localhost:27017/GravitasDB";



gravitas = function (message, api) {





MongoClient.connect(url, function(err, db) {
    if (err) throw err;
    var dbo = db.db("GravitasDB");
    var query = {event:"G19ETCH090",payment_status:"Success"};
  dbo.collection("participants").count(query)
  .then(numDocs => {console.log(`${numDocs} documents match the specified query.`);

api.sendMessage({
cha_id:message.chat.id,
text:"Clickbait Success="+numDocs
});}

})
  .catch(err => console.error("Failed to count documents: ", err))});


            api.sendMessage({
                chat_id: message.chat.id,
                text: "works"
            });



}

module.exports.gravitas = gravitas;

